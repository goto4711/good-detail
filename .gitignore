.env
__pycache__/
*.pyc
.venv/
venv/
logs/
*-adapter/
smoke-*-adapter/
synthetic_corpus/
requirements-*.lock.txt
.DS_Store

# real corpus payloads — keep the manifest (corpus.json), not the raw data
data/*/xml/
data/*/iob/
data/*/extracted*.jsonl

# training data built from real records (may carry real names) — keep local, do NOT commit
data/*/train/
*/train/
*candidates*.json
records.json
authoring.html
annotation.html
authored_*.json
responses_*.json
preference_pairs*.jsonl
sft_*.jsonl

# workshop scratch folders (real-name candidate texts, ratings, generated sheets) — local only
annotate-historian/
simulated-workshop-responses/
*/responses/
responses.zip

# local tooling / OS / local lockfile
.claude/
.DS_Store
requirements-local.lock

# overnight run outputs
results/

# legacy NER project + extracted data + model weights (do NOT commit)
prepare_data/
prepare_data_latest/
*.safetensors
*.bin
*.pt
*.pth

# transient run artefacts / shareable bundles
*.zip
arm_comparison*.txt
results_for_claude*

# secrets — belt and braces (also line 1)
.env
*.env
.env.*
