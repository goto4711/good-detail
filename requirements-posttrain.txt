# Pinned post-training stack for the "good detail" smoke test.
# ----------------------------------------------------------------------
# Targets the TRL >= 1.0 API (unified SFT / DPO / KTO / GRPO, per-trainer
# config classes, processing_class). These are STARTING constraints, not
# verified-frozen versions: install on Snellius, get a green run, then
#   pip freeze > requirements-posttrain.lock.txt
# and use the lock file for true reproducibility (same versions on laptop
# and GPU node -- this is what stops "works here, breaks there").
#
# Install (GPU node):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-posttrain.txt
# CUDA build of torch may need the official index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# ----------------------------------------------------------------------

torch>=2.4
transformers>=4.46
trl>=1.0.0
peft>=0.13
accelerate>=0.34
datasets>=3.0

# optional: 4-bit quantisation (QLoRA) on CUDA
bitsandbytes>=0.44 ; platform_system == "Linux"
